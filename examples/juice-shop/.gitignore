# Generated by setup.sh and by scan runs. The reviewable plan lives at
# examples/juice-shop/plan.json; .trinker/ only holds the copy the CLI reads plus the
# gitignored runtime config and reports.
.trinker/
report.json
verify*.md
